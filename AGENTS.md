# 项目约定

- 使用可追溯的既有教学案例及其配套数据。用户明确允许案例材料本身经过模拟、匿名化或改写；禁止本项目自行生造经营记录，缺失数据说明边界。
- 主课程为 Berkeley Haas Rocket Fuel B5894；Kellogg Air France KEL319 / KEL321 的旧代码、网站、资料独立保存在 dist/reference/air-france 与 docs/reference-air-france，不合并两个企业的数据。镜像来源必须标明，不自动认证为官方原件。
- 原始数据与派生明细仅放在 data/private，禁止提交、上传或打包进 dist。
- 课程题目在 dist/assets/course.mjs 维护，使用 scripts/export-course.mjs 同步文档。四题是基于实际案例的教学改编，不声称是出版方原题或标准答案。
- 计算集中在 analytics.mjs 和 investigation.mjs 的纯函数中，先求和再求比率。保留原始单元格与物理源行号。异常记录不可静默删除；核心质量异常时暂停因果推断。
- 保留独立初判、证据提交、跨题引用、指标版本与数据指纹校验。修改前题、指标或相关情景后，后题需重新提交；不能覆盖或删除已有文字。新旧课程存储隔离，导入小组新增副本。
- 采用讲解模式：先讲案例故事和经营困境，再展示详细问题、完整参考答案与可视化推导。答案和交互不受提交状态限制。
- 案例参数、数据观测与学员假设分开标注。收入减广告费不是利润；增量净贡献不是企业净利润。频次/时段关联不能当作策略因果效应。
- 修改核心计算后运行 npm test；修改静态资源后运行 npm run check。保持文件导入、浏览器本地保存及无数据状态可用。
- 仅使用本地 Web 服务（默认 http://127.0.0.1:4317/），不发布或托管到 Sites，除非用户明确改变要求。
- 每次完成项目变更后，实际请求本地首页与受影响页面资源；未运行时先启动。修改服务端代码后重启并复查，响应正常后交付。
