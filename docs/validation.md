# 实现与验证记录

- 原始文件通过固定 Git blob、SHA-256、字节数、六列结构、唯一用户与缺失检查；未修改原始记录。
- 采用 Node 纯函数重算完整文件，检查分组、频次、星期和小时切片守恒。
- 使用 Python 标准 CSV 阅读器独立聚合；statsmodels 0.15.0 的 Newcombe 区间结果与网站一致。
- 样本规划在 50%、20%、4% 对照比例下，与 statsmodels 双侧 5%、功效 80% 的近似公式一致。验证中的 MDE 0.5 个百分点是测试规划参数，不是案例观测或课程推荐阈值。
- `scripts/validate-rocket.py` 可复现独立验证；需可用的 statsmodels、scipy。网站和 Node 测试不依赖这些 Python 库。
- 47 项 Node 测试覆盖原 Air France、Rocket Fuel 原始文件、无数据呈现、安全公式、经济核算、样本规划、证据保留/失效，以及跨环境数据加载和代理下载。
- 浏览器检查：完整文件自动读取和手动导入；自定义跨组指标；本地保存；前题修改使后题提示重交但保留原文字；成本情景更新；时段探索；MDE/分流交互；原始 PSA 购买用户筛选 420 行及分页；小组导入新增副本。
- 手机首页在 390px 下无页面水平溢出。旧站可通过独立入口进入，并读取原 Air France 数据。
- 导出由浏览器发给同源本机服务，保存在 data/private/exports 独立目录，再提供下载链接；不上传至远端。该设计避免依赖内置浏览器对临时 Blob 下载的处理。

2026-09-16 跨环境数据加载修复：

- 下载器测试覆盖空目录准备、正确文件离线复用、不匹配文件保留、下载不完整、错误指纹、超大响应、HTTP/网络/超时/流中断，以及并发产生文件时不覆盖。
- 另在 data/private 下的空验收目录真实下载固定镜像，完整取得 12,024,311 字节并通过 SHA-256；验收副本随后清理，原有课程数据保持原样。
- 浏览器加载测试覆盖静态 404、HTML 首页回退、镜像请求失败、完整字节数与指纹校验、手动文件导入路径。
- 在仅提供 dist 的本地静态服务器上实测：自动读取提示数据缺失；点击“从公开镜像加载”后显示 588,101 条记录且镜像指纹一致。原始明细未打包进 dist。
- 在非项目工作目录启动 Node 服务，首页和完整数据端点均返回 200；项目路径不再依赖当前工作目录。
- 默认本地服务实测首页、修改后的模块、旧案例入口与主案例 CSV 均返回 200，HTTP 返回的数据与固定 SHA-256 一致。

后续每次变更仍需按 AGENTS.md 运行适当检查、检查本地服务，修改服务端后重启。没有 Sites 发布动作。

2026-09-17 npm 代理下载修复：

- 通过本地 HTTP 代理实际下载传输测试文件，核对请求目标与代理认证；日志不包含代理地址或凭据。
- 通过本地 HTTPS 源与 CONNECT 代理传输完整案例文件，仍匹配 12,024,311 字节与固定 SHA-256。TLS 源使用临时测试证书：未配置可信 CA 时拒绝下载，显式配置测试 CA 后通过，未关闭证书校验。
- 实测 SOCKS5h 将目标域名交给代理解析；NO_PROXY 可以绕过代理。验证配置优先级与大小写归一化，避免 Windows 子进程环境变量大小写重复。
- 验证 HTTP 407、连接中断 / 超时、curl 缺失与 Node 通用 fetch failed 的提示。失败清理临时文件，不偷偷切换为直连。
- 代理集成测试使用系统 curl，HTTPS 测试证书由 openssl 临时生成；缺少相应工具时该项测试会显式跳过。本次运行 47 项全部通过，无跳过。
- 以上为本地受控代理验证；用户其他环境的实际代理地址、端口及网络策略尚未提供，不能据此声称已验证那一环境。
